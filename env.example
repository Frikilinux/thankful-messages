# Copiar a un archivo .env usado por spring-dotenv sólo en tiempo de ejecución
# y por compose.yml para la configuración de docker-compose
#
# Reemplazar con tus propios valores

# Spring boot configuración
DB_HOST = postgres:5432
DB_NAME = thankful-mesagges
DB_USERNAME = thankful
DB_PASSWORD = messages
PORT=59376

# Docker configuración
# No modificar lo siguiente
POSTGRES_DB=${DB_NAME}
POSTGRES_USER=${DB_USERNAME}
POSTGRES_PASSWORD=${DB_PASSWORD}
